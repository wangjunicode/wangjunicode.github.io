---
title: Unity帧同步
published: 2019-05-28
description: "服务器在当帧收集所有客户端上报，再将所有客户端的输入，同步给每个客户端"
tags: []
category: 网络同步
draft: false
---



### 帧同步的基本原理




![image-20230905205447095](/images/posts/关于游戏战斗开发-帧同步/image-20230905205447095.png)



服务器在当帧收集所有客户端上报，再将所有客户端的输入，同步给每个客户端



**严格帧同步：**

等待所有客户端上报，如果有客户端没上报，流程就会卡主



**帧同步优化**

- 如果有客户端没输入，不等待，流程继续往下走
- 客户端收集输入上报服务器的tick，没必要和服务器tick一样，可以短也可以长
- 客户端发送服务器的指令丢包导致操作无效，但游戏能进行下去，但是服务器发送的统计到所有人指令是必须要确保所有客户端都收到，不然就会出现严重的不同步，游戏进行不下去。所以需要可靠的UDP，有个KCP的开源库可以支持。但其实帧同步的框架本身是支持这种问题的，如果收到的服务器包不是当帧+1，那就认为丢包了，就可以让服务器重新发送那帧的包
- 客户端断线重连时，服务器将开始到当前帧的所有保存状态都发下来，客户端在本地执行所有帧并跟上



**手感优化**

手游的网络情况比较复杂，丢包，延迟以及帧同步本身存在的延迟情况，导致优化画面或手感存在很大的问题，我们只能极大程度地弱化网络导致的画面卡顿情况。

- 预测：对于一个30帧的帧同步来说，最理想的情况是客户端每0.03s收到一次服务器发送过来的帧信息来执行客户端的update，如果没及时收到，可以跑空帧，直接执行上一帧进行预测，当然这里只预测跟运动相关的东西，战斗相关的信息（子弹，技能，buff）这些就不需要了
- 回滚：有预测就有回滚。显示的位置不在实际位置时候，插值到实际位置
- 预表现：客户端在按下某个技能，需要等到服务器接收，然后转发到自己接收，才能执行，这期间大概有0.1-0.2s的时间，可以针对你操控的角色在按下命令时做一些起手动作，转身动作，播放音效等。

**逻辑和显示分离**

**定点数**

**工具链**

- 不同步信息查看工具
- 录像播放工具
- 自动战斗工具

不同步问题：不要使用不确定顺序的容器

# 帧同步的一些思考（二）：平滑位移与UDP通信

## 平滑位移

客户端影子追随算法

- 渲染球追着逻辑球位移
- 逻辑球没更新，则渲染球保持逻辑球当前速度继续做位移（当前速度为0，即为停止状）

这样在可以接受的网络中，画面可以保持流畅。上述影子追随算法，在帧同步游戏中同样适用。

帧同步游戏本身就是游戏逻辑和渲染逻辑分离的，影子追随算法也是这样。

## 可以接受的网络

一般在100-200ms延迟的网络可以接受。

## 帧同步中，在网络上可能遇到的问题

### 多个客户端如何同时开始游戏

根据影子追随算法，画面始终在平滑过渡到最新收到的网络包。因此在可接受的网络中，多个客户端画面也是基本一致的，看不出差别的。

### 各种原因导致，包延迟到达客户端，怎么办

影子追随自带预测，能很好平滑过渡延迟到达的包

### 包提早到达？

客户端需要缓存这个包，先同步到上一帧的状态，再执行下一帧

## UDP通信

- 可靠UDP：对实时性要求不是非常高的游戏，有很多成熟库KCP
- 纯UDP：高实时性游戏，以下讨论纯UDP

**当客户端UDP发送操作指令给服务器**

网络好的玩家单发UDP包，网络不好的玩家才使用双发UDP包

一般服务器66ms或100ms；这里假设66ms一次，也就是一秒钟15帧，那么客户端可以按30ms频率重复发送当前指令，直到收到服务器ACK包，或者收到服务器发下来的下一帧，或者指令被替换

**服务器UDP转发操作指令给 客户端**

方法1：客户端收到UDP包后，均会ACK服务器，服务器每次UDP包携带前ACK的UDP一并冗余发送。若超过548字节，则分多个包发送

方法2：服务器冗余前N帧数据，客户端在缺帧时，向服务器请求某帧

## 乐观锁

- 传统情况，每个客户端必须回报给服务器收到帧数，服务器再次发送确认包才执行帧数据，否则所有人等待。
- 现在情况，基于现在手游的流行程度和若网络环境，手游一般都采取乐观锁模式。即收到服务器推帧后，客户端立即执行，不等待其他人。这样卡顿的人自己卡，不影响其他人的游戏体验。同时卡顿的人在收到数据后，自行加速补帧，追赶上正确的游戏速度。

参考

[帧同步游戏在技术层面的实现细节](https://zhuanlan.zhihu.com/p/408734657)

[帧同步游戏开发小结](https://www.cnblogs.com/xiaohutu/p/12402399.html)

[漫话帧同步](https://zhuanlan.zhihu.com/p/381339755)
