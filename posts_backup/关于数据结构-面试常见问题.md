---
title: 关于数据结构-面试常见的数据结构问题
published: 2017-09-11
description: "关于数据结构-面试常见的数据结构问题"
tags: []
category: 基础知识
draft: false
---

关于数据结构-面试常见的数据结构问题

# 二叉树的遍历

## 重点总结

二叉树的遍历方式有四种

深度优先遍历有三种：这三种的循环遍历方法中都用到了栈stack。不建议采用递归的原因在于如果太深，会有可能导致栈溢出

前序遍历：根节点，左子树，右子树

中序遍历：左子树，根节点，右子树

后序遍历：左子树，右子树，根节点

广度优先遍历

层序遍历：从上到下，从左到右，这种方式用到了队列（queue）：先进先出

```c#
public class Tree
{
    public static void PreSort(TreeNode node)
    {
        if (node == null)
        {
            return;
        }

        Console.WriteLine(node.value);
        PreSort(node.leftChild);
        PreSort(node.rightChild);
    }

    public static void PreSortLoop(TreeNode node)
    {
        Stack<TreeNode> stack = new Stack<TreeNode>();
        while (node != null || stack.Count > 0)
        {
            if (node != null)
            {
                stack.Push(node);
                Console.WriteLine(node.value);
                node = node.leftChild;
            }
            else
            {
                var tempNode = stack.Pop();
                node = tempNode.rightChild;
            }
        }
    }
    
    public static void LevelOrder(TreeNode _node)
    {
        Queue<TreeNode> queue = new Queue<TreeNode>();
        TreeNode node = _node;
        queue.Enqueue(node);
        while(queue.Count > 0)
        {
            var tempNode = queue.Dequeue();
            Log(tempNode.value);
            if(tempNode.leftChild != null)
            {
                queue.Enqueue(tempNode.leftChild);
            }
            if(tempNode.rightChild != null)
            {
                queue.Enqueue(tempNode.rightChild);
            }
        }
    }

    public class TreeNode
    {
        public int value;
        public TreeNode leftChild;
        public TreeNode rightChild;
        public TreeNode(int v)
        {
            value = v;
        }
    }
}
```

## 实现二叉树，详细的功能实现都要写出来

https://blog.csdn.net/qq_34035956/article/details/104036671

```c#
//面试问能写点代码吗？ 实现个二叉树吧

namespace StructScript
{
	public class BinaryTree
	{
		public TreeNode root;
		public BinaryTree(int[] arr)
		{
			for(int i = 0; i < arr.Length; i++)
			{
				AddNode(arr[i]);
			}
		}
		private void AddNode(int value)
		{
			TreeNode curNode = root;
			if(root == null)
			{
				root = new TreeNode(value);
				return;
			}
			
			while(true)
			{
				if(value < curNode.value)
				{
					if(curNode.left == null)
					{
						curNode.left = new TreeNode(value);
						return;
					}
					else
					{
						curNode = curNode.left;
					}
				}
				else
				{
					if(curNode.right == null)
					{
						curNode.right = new TreeNode(value);
						return;
					}
					else
					{
						curNode = curNode.right;
					}
				}
			}
		}
		
		public void PreOrder(TreeNode root)
		{
			if(root != null)
			{
				Console.WriteLine(root.value);
				PreOrder(root.left);
				PreOrder(root.right);
			}
		}
		
		public void PreOrderWithStack(TreeNode root)
		{
			Stack<TreeNode> stack = new Stack<TreeNode>();
			var curNode = root;
			while(curNode != null || stack.Count > 0)
			{
				if(curNode != null)
				{
					stack,push(curNode);
					Console.WriteLine(curNode.value);
					curNode = curNode.left;
				}
				else
				{
					var tempNode = stack.Pop();
					curNode = tempNode.right;
				}
			}
		}
	}
	
	pubic class TreeNode
	{
		public int value;
		public TreeNode left;
		public TreeNode right;
		public TreeNode(int value, TreeNode left, TreeNode right)
		{
			this.value = value;
			this.left = left;
			this.right = right;
		}
	}
}
```

